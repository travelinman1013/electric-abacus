# Firebase Admin SDK Configuration
#
# This file is a template for environment-specific .env files.
# Copy this to .env.production or .env.staging and fill in your credentials.
#
# USAGE:
#   - These credentials are used by server-side scripts (seed, migration)
#   - DO NOT commit actual .env files to git (they're in .gitignore)
#   - Get these values from Firebase Console → Project Settings → Service Accounts
#
# STEPS TO GET CREDENTIALS:
#   1. Go to Firebase Console: https://console.firebase.google.com
#   2. Select your project
#   3. Go to Project Settings (gear icon) → Service Accounts
#   4. Click "Generate new private key"
#   5. Download the JSON file
#   6. Extract values from JSON and paste below

# Project ID (from Firebase Console)
FIREBASE_PROJECT_ID=your-project-id

# Service account email (from service account JSON)
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com

# Private key (from service account JSON)
# IMPORTANT: Keep the quotes and newlines (\n) exactly as shown
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"
